# MineEcho Console local-first defaults.
# Copy this file to .env only when you need to override local development behavior.

# Local development bypasses the login screen by default.
# Set to true when explicitly testing the auth flow.
VITE_MINEECHO_AUTH_REQUIRED=false
